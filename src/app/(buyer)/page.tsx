import HomePage from '@/components/pages/home/home-page';
import React from 'react';

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <HomePage />
    </div>
  );
};

export default Home;
